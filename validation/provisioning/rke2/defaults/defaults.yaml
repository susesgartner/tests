#Required for the template test
templateTest:
  repo:
    metadata:
      name: "test"
    spec:
      gitRepo: "<required>"
      gitBranch: "<required>"
      insecureSkipTLSVerify: true
  templateProvider: "aws"
  templateName: "<required>"