#Required for all tests
rancher:
  host: "<required>"
  adminToken: "<required>"
  insecure: true
  cleanup: true

prime:
  brand: "<required>"
  isPrime: true 
  rancherVersion: "<required>"
  registry: "<required>"