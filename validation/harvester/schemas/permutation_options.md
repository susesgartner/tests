* select engine (rke2, rke1, k3s)
* select cluster type (single node, stacked, HA, or production)
* select CNI (canal, flannel, calico)\n * select kubernetes version
* enable/disable cloud provider (disabled by default) 